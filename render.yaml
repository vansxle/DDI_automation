services:
  - type: web
    name: ddi-automation
    runtime: python
    buildCommand: sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz0b libfontconfig1 libcairo2 libgdk-pixbuf2.0-0 && pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
